[user]
	email = jstewart101@gmail.com
	name = amorphic
[color]
	ui = true
[credential]
	helper = cache --timeout=3600
[core]
	editor = vim
	excludesfile = ~/.gitignore
[push]
	default = matching 
	default = simple
[alias]
    hist = log --graph --pretty=format:\"%C(yellow)%h%C(reset)%C(auto)%d%C(reset) %s %C(white)%an, %ar%C(reset)\"
    punt = push origin master develop --tags
    st = status
    cp = cherry-pick
    co = checkout
